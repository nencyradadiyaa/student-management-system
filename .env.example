# Server Configuration
PORT=3000
NODE_ENV=development

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=student_management

# Express Session Secret
SESSION_SECRET=super_secret_session_key_for_student_management_system_123

# Email / SMTP Configuration (for notifications)
# Set up values to receive real email updates, or leave as mock defaults.
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=mock_user
SMTP_PASS=mock_pass
SMTP_FROM="SMS Administrator" <admin@college.edu>
